package reika.rotarycraft.base;

// 1.21.5 STUB: RenderType is no longer subclassable (private constructor) and the entire
// CompositeState/RenderStateShard pipeline was replaced by RenderPipelines + RenderSetup.
// Custom render types should be built via RenderType.create(name, RenderSetup) instead.
public final class RotaryRenderTypes {
    private RotaryRenderTypes() {}
}
